const name = "lee";

const age = 30;

const hasJob = true;

const car = null;

let anything;

const sym = Symbol();

const hobbies = ["books", "walking"];

const address = {
    province: "서울",
    city: "서초구",
};

console.log(typeof hobbies);
console.log(Array.isArray(hobbies));
console.log(Array.isArray(address));
